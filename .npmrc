# Strict node engine matching — fail install if Node version is outside the
# range declared in package.json. Catches "works on my machine" drift before
# CI does.
engine-strict=true

# Hoist nothing implicit — pnpm's strict isolation by default catches
# accidentally-imported transitive deps. Lift this only if a specific package
# breaks (and document why).
shamefully-hoist=false
