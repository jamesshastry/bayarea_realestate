[project]
# Placeholder so the `packages/*` workspace glob can resolve.
# Owned by the ETL track (other agent) — do not extend without that owner.
name = "bayre-etl"
version = "0.0.0"
description = "Placeholder for the ETL package; owned by another agent track."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
bypass-selection = true
